---
apiVersion: v1
kind: Secret
metadata:
  name: core-db-primarydbsource
  namespace: openlane-secret-push
type: Opaque
data:
  # Base64 encode your secret value and paste it here
  # Example: echo -n "your-secret-value" | base64
  CORE_DB_PRIMARYDBSOURCE: ""
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: core-db-primarydbsource-push
  namespace: openlane-secret-push
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: gcp-secretstore
      kind: ClusterSecretStore
  updatePolicy: Replace
  deletionPolicy: Delete
  selector:
    secret:
      name: core-db-primarydbsource  # References the Secret above
  data:
    - match:
        secretKey: CORE_DB_PRIMARYDBSOURCE  # The key from the Secret above
        remoteRef:
          remoteKey: core-db-primarydbsource  # The destination secret name in GCP Secret Manager
          property: CORE_DB_PRIMARYDBSOURCE  # The key within the destination secret object (matches secretKey)
