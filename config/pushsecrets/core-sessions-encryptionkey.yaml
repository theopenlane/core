---
apiVersion: v1
kind: Secret
metadata:
  name: core-sessions-encryptionkey
  namespace: openlane-secret-push
type: Opaque
data:
  # Base64 encode your secret value and paste it here
  # Example: echo -n "your-secret-value" | base64
  CORE_SESSIONS_ENCRYPTIONKEY: ""
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: core-sessions-encryptionkey-push
  namespace: openlane-secret-push
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - name: gcp-secretstore
      kind: ClusterSecretStore
  updatePolicy: Replace
  deletionPolicy: Delete
  selector:
    secret:
      name: core-sessions-encryptionkey  # References the Secret above
  data:
    - match:
        secretKey: CORE_SESSIONS_ENCRYPTIONKEY  # The key from the Secret above
        remoteRef:
          remoteKey: core-sessions-encryptionkey  # The destination secret name in GCP Secret Manager
          property: CORE_SESSIONS_ENCRYPTIONKEY  # The key within the destination secret object (matches secretKey)
