// Package urlx provides shared URL parsing, normalization, and validation
// helpers, along with token URL construction and shortening utilities for
// embedding signed tokens in email action links
package urlx
