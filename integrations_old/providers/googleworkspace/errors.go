package googleworkspace

import "errors"

var (
	// errGoogleWorkspaceAdminServiceClientBuild indicates a failure building the Admin SDK service client
	errGoogleWorkspaceAdminServiceClientBuild = errors.New("googleworkspace: failed to build admin service client")
)
