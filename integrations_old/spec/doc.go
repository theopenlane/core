// Package spec defines operator-configurable provider specifications for the integrations
// subsystem. Specs are read from embedded JSON files or overridden via koanf/env vars and
// describe authentication, credential, and persistence configuration for each provider.
package spec
