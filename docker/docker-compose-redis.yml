services:
  redis:
    image: redis:7.4.2-alpine
    restart: always
    ports:
      - '6381:6379'
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - 'redis_data:/data'
volumes:
  redis_data:
    driver: local
