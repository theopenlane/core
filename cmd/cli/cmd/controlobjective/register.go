//go:build cli

package controlobjective

import (
	"embed"
	"fmt"
	"reflect"

	"github.com/theopenlane/core/cmd/cli/internal/speccli"
	"github.com/theopenlane/core/pkg/openlaneclient"
)

//go:embed spec.json
var specFS embed.FS

func init() {
	typeMap := map[string]reflect.Type{
		"openlaneclient.CreateControlObjectiveInput": reflect.TypeOf(openlaneclient.CreateControlObjectiveInput{}),
		"openlaneclient.UpdateControlObjectiveInput": reflect.TypeOf(openlaneclient.UpdateControlObjectiveInput{}),
	}

	if err := speccli.RegisterFromFS(specFS, speccli.LoaderOptions{
		TypeResolver: speccli.StaticTypeResolver(typeMap),
		Parsers:      speccli.DefaultParsers(),
	}); err != nil {
		panic(fmt.Sprintf("failed to register controlobjective command: %v", err))
	}
}
