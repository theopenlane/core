//go:build cli

package evidence

import (
	"embed"
	"fmt"
	"reflect"

	"github.com/theopenlane/core/cmd/cli/internal/speccli"
	"github.com/theopenlane/core/pkg/openlaneclient"
)

//go:embed spec.json
var specFS embed.FS

func init() {
	typeMap := map[string]reflect.Type{
		"openlaneclient.CreateEvidenceInput": reflect.TypeOf(openlaneclient.CreateEvidenceInput{}),
		"openlaneclient.UpdateEvidenceInput": reflect.TypeOf(openlaneclient.UpdateEvidenceInput{}),
	}

	if err := speccli.RegisterFromFS(specFS, speccli.LoaderOptions{
		TypeResolver: speccli.StaticTypeResolver(typeMap),
		Parsers:      speccli.DefaultParsers(),
	}); err != nil {
		panic(fmt.Sprintf("failed to register evidence command: %v", err))
	}
}
