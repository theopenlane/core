//go:build cli

package tokens

import (
	"embed"
	"fmt"
	"reflect"

	"github.com/theopenlane/core/cmd/cli/internal/speccli"
	"github.com/theopenlane/core/pkg/openlaneclient"
)

//go:embed spec.json
var specFS embed.FS

func init() {
	typeMap := map[string]reflect.Type{
		"openlaneclient.CreatePersonalAccessTokenInput": reflect.TypeOf(openlaneclient.CreatePersonalAccessTokenInput{}),
		"openlaneclient.UpdatePersonalAccessTokenInput": reflect.TypeOf(openlaneclient.UpdatePersonalAccessTokenInput{}),
	}

	if err := speccli.RegisterFromFS(specFS, speccli.LoaderOptions{
		TypeResolver: speccli.StaticTypeResolver(typeMap),
		Parsers:      speccli.DefaultParsers(),
	}); err != nil {
		panic(fmt.Sprintf("failed to register personal access token command: %v", err))
	}
}
