//go:build cli

package entitytype

import (
	"embed"
	"fmt"
	"reflect"

	"github.com/theopenlane/core/cmd/cli/internal/speccli"
	"github.com/theopenlane/core/pkg/openlaneclient"
)

//go:embed spec.json
var specFS embed.FS

func init() {
	typeMap := map[string]reflect.Type{
		"openlaneclient.CreateEntityTypeInput": reflect.TypeOf(openlaneclient.CreateEntityTypeInput{}),
		"openlaneclient.UpdateEntityTypeInput": reflect.TypeOf(openlaneclient.UpdateEntityTypeInput{}),
	}

	if err := speccli.RegisterFromFS(specFS, speccli.LoaderOptions{
		TypeResolver: speccli.StaticTypeResolver(typeMap),
		Parsers:      speccli.DefaultParsers(),
	}); err != nil {
		panic(fmt.Sprintf("failed to register entitytype command: %v", err))
	}
}
