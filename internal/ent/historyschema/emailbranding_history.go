// Code generated by entx.history, DO NOT EDIT.
package historyschema

import (
	"time"

	"entgo.io/contrib/entgql"
	"entgo.io/ent"
	"entgo.io/ent/dialect/entsql"
	entschema "entgo.io/ent/schema"
	"entgo.io/ent/schema/field"
	"entgo.io/ent/schema/index"

	"github.com/theopenlane/core/internal/ent/interceptors"
	"github.com/theopenlane/core/internal/ent/privacy/policy"
	"github.com/theopenlane/core/internal/ent/schema"
	"github.com/theopenlane/entx"
	"github.com/theopenlane/entx/history"
)

// EmailBrandingHistory holds the schema definition for the EmailBrandingHistory entity.
type EmailBrandingHistory struct {
	ent.Schema
}

// Annotations of the EmailBrandingHistory.
func (EmailBrandingHistory) Annotations() []entschema.Annotation {
	return []entschema.Annotation{
		entx.SchemaGenSkip(true),
		entsql.Annotation{
			Table: "email_branding_history",
		},
		history.Annotations{
			IsHistory: true,
			Exclude:   true,
		},
		entgql.QueryField(),
		entgql.RelayConnection(),
	}
}

// Fields of the EmailBrandingHistory.
func (EmailBrandingHistory) Fields() []ent.Field {
	historyFields := []ent.Field{
		field.Time("history_time").
			Annotations(
				entgql.OrderField("history_time"),
			).
			Default(time.Now).
			Immutable(),
		field.String("ref").
			Immutable().
			Optional(),
		field.Enum("operation").
			GoType(history.OpType("")).
			Immutable(),
	}

	// get the fields from the mixins
	// we only want to include mixin fields, not edges
	// so this prevents FKs back to the main tables
	mixins := schema.EmailBranding{}.Mixin()
	for _, mixin := range mixins {
		for _, field := range mixin.Fields() {
			// make sure the mixed in fields do not have unique constraints
			field.Descriptor().Unique = false

			// make sure the mixed in fields do not have validators
			field.Descriptor().Validators = nil

			// append the mixed in field to the history fields
			historyFields = append(historyFields, field)
		}
	}

	original := schema.EmailBranding{}
	for _, field := range original.Fields() {
		// make sure the fields do not have unique constraints
		field.Descriptor().Unique = false

		// make sure the mixed in fields do not have validators
		field.Descriptor().Validators = nil

		// append the field to the history fields
		historyFields = append(historyFields, field)
	}

	return historyFields
}

// Indexes of the EmailBrandingHistory
func (EmailBrandingHistory) Indexes() []ent.Index {
	return []ent.Index{
		index.Fields("history_time"),
	}
}

// Policy of the EmailBrandingHistory.
// ensure history.AllowIfHistoryRequest() is already added to the base policy
func (EmailBrandingHistory) Policy() ent.Policy {
	return policy.NewPolicy(
		policy.WithMutationRules(
			history.AllowIfHistoryRequest(),
		),
	)
}

// Interceptors of the EmailBrandingHistory
func (EmailBrandingHistory) Interceptors() []ent.Interceptor {
	return []ent.Interceptor{
		interceptors.HistoryAccess("audit_log_viewer", false, false, ""),
	}
}
