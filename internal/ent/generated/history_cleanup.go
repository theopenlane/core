//go:build !codegen

// Code generated by ent, DO NOT EDIT.

package generated

import (
	"context"

	"entgo.io/ent/dialect/sql"
	"github.com/theopenlane/core/v2/internal/ent/generated/actionplan"
	"github.com/theopenlane/core/v2/internal/ent/generated/assessment"
	"github.com/theopenlane/core/v2/internal/ent/generated/assessmentresponse"
	"github.com/theopenlane/core/v2/internal/ent/generated/asset"
	"github.com/theopenlane/core/v2/internal/ent/generated/audience"
	"github.com/theopenlane/core/v2/internal/ent/generated/audiencemember"
	"github.com/theopenlane/core/v2/internal/ent/generated/campaign"
	"github.com/theopenlane/core/v2/internal/ent/generated/campaigntarget"
	"github.com/theopenlane/core/v2/internal/ent/generated/contact"
	"github.com/theopenlane/core/v2/internal/ent/generated/control"
	"github.com/theopenlane/core/v2/internal/ent/generated/controlimplementation"
	"github.com/theopenlane/core/v2/internal/ent/generated/controlobjective"
	"github.com/theopenlane/core/v2/internal/ent/generated/customdomain"
	"github.com/theopenlane/core/v2/internal/ent/generated/discussion"
	"github.com/theopenlane/core/v2/internal/ent/generated/documentdata"
	"github.com/theopenlane/core/v2/internal/ent/generated/emailtemplate"
	"github.com/theopenlane/core/v2/internal/ent/generated/entity"
	"github.com/theopenlane/core/v2/internal/ent/generated/entitytype"
	"github.com/theopenlane/core/v2/internal/ent/generated/evidence"
	"github.com/theopenlane/core/v2/internal/ent/generated/file"
	"github.com/theopenlane/core/v2/internal/ent/generated/finding"
	"github.com/theopenlane/core/v2/internal/ent/generated/findingcontrol"
	"github.com/theopenlane/core/v2/internal/ent/generated/group"
	"github.com/theopenlane/core/v2/internal/ent/generated/groupmembership"
	"github.com/theopenlane/core/v2/internal/ent/generated/groupsetting"
	"github.com/theopenlane/core/v2/internal/ent/generated/hush"
	"github.com/theopenlane/core/v2/internal/ent/generated/identityholder"
	"github.com/theopenlane/core/v2/internal/ent/generated/internalpolicy"
	"github.com/theopenlane/core/v2/internal/ent/generated/mappabledomain"
	"github.com/theopenlane/core/v2/internal/ent/generated/mappedcontrol"
	"github.com/theopenlane/core/v2/internal/ent/generated/narrative"
	"github.com/theopenlane/core/v2/internal/ent/generated/note"
	"github.com/theopenlane/core/v2/internal/ent/generated/notificationpreference"
	"github.com/theopenlane/core/v2/internal/ent/generated/notificationtemplate"
	"github.com/theopenlane/core/v2/internal/ent/generated/organization"
	"github.com/theopenlane/core/v2/internal/ent/generated/organizationsetting"
	"github.com/theopenlane/core/v2/internal/ent/generated/orgmembership"
	"github.com/theopenlane/core/v2/internal/ent/generated/platform"
	"github.com/theopenlane/core/v2/internal/ent/generated/predicate"
	"github.com/theopenlane/core/v2/internal/ent/generated/procedure"
	"github.com/theopenlane/core/v2/internal/ent/generated/program"
	"github.com/theopenlane/core/v2/internal/ent/generated/programmembership"
	"github.com/theopenlane/core/v2/internal/ent/generated/remediation"
	"github.com/theopenlane/core/v2/internal/ent/generated/review"
	"github.com/theopenlane/core/v2/internal/ent/generated/risk"
	"github.com/theopenlane/core/v2/internal/ent/generated/sladefinition"
	"github.com/theopenlane/core/v2/internal/ent/generated/standard"
	"github.com/theopenlane/core/v2/internal/ent/generated/subcontrol"
	"github.com/theopenlane/core/v2/internal/ent/generated/subprocessor"
	"github.com/theopenlane/core/v2/internal/ent/generated/systemdetail"
	"github.com/theopenlane/core/v2/internal/ent/generated/task"
	"github.com/theopenlane/core/v2/internal/ent/generated/template"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcenter"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcentercompliance"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcenterdoc"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcenterentity"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcenterfaq"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcenterndarequest"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcentersetting"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcentersubprocessor"
	"github.com/theopenlane/core/v2/internal/ent/generated/trustcenterwatermarkconfig"
	"github.com/theopenlane/core/v2/internal/ent/generated/user"
	"github.com/theopenlane/core/v2/internal/ent/generated/usersetting"
	"github.com/theopenlane/core/v2/internal/ent/generated/vendorriskscore"
	"github.com/theopenlane/core/v2/internal/ent/generated/vendorscoringconfig"
	"github.com/theopenlane/core/v2/internal/ent/generated/vulnerability"
	"github.com/theopenlane/core/v2/internal/ent/generated/workflowassignment"
	"github.com/theopenlane/core/v2/internal/ent/generated/workflowassignmenttarget"
	"github.com/theopenlane/core/v2/internal/ent/generated/workflowdefinition"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/actionplanhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/assessmenthistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/assessmentresponsehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/assethistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/audiencehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/audiencememberhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/campaignhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/campaigntargethistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/contacthistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/controlhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/controlimplementationhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/controlobjectivehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/customdomainhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/discussionhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/documentdatahistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/emailtemplatehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/entityhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/entitytypehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/evidencehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/filehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/findingcontrolhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/findinghistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/grouphistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/groupmembershiphistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/groupsettinghistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/hushhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/identityholderhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/internalpolicyhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/mappabledomainhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/mappedcontrolhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/narrativehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/notehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/notificationpreferencehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/notificationtemplatehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/organizationhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/organizationsettinghistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/orgmembershiphistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/platformhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/procedurehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/programhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/programmembershiphistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/remediationhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/reviewhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/riskhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/sladefinitionhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/standardhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/subcontrolhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/subprocessorhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/systemdetailhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/taskhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/templatehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcentercompliancehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcenterdochistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcenterentityhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcenterfaqhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcenterhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcenterndarequesthistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcentersettinghistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcentersubprocessorhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/trustcenterwatermarkconfighistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/userhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/usersettinghistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/vendorriskscorehistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/vendorscoringconfighistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/vulnerabilityhistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/workflowassignmenthistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/workflowassignmenttargethistory"
	"github.com/theopenlane/core/v2/internal/ent/historygenerated/workflowdefinitionhistory"
	"github.com/theopenlane/core/v2/internal/ent/hooks/contextx"
	"github.com/theopenlane/core/v2/pkg/logx"
	"github.com/theopenlane/entx/history"
)

// PurgeHistoryEnabled reports whether the current operation is purging history rather than recording
// it. The generated history hooks use this as their skipper, a delete that is purging history must
// not write a new row recording that delete, it would undo the purge that ran just before it
func PurgeHistoryEnabled(ctx context.Context) bool {
	return contextx.PurgeHistoryEnabled(ctx)
}

// PurgeActionPlanHistory removes the history rows belonging to every actionplan matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the actionplan records themselves are deleted, the rows are matched
// with a sub-select against the actionplan table
func PurgeActionPlanHistory(ctx context.Context, ps ...predicate.ActionPlan) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(actionplan.FieldID).From(sql.Table(actionplan.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ActionPlanHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(actionplanhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging actionplan history")

		return err
	}

	return nil
}

// PurgeAssessmentHistory removes the history rows belonging to every assessment matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the assessment records themselves are deleted, the rows are matched
// with a sub-select against the assessment table
func PurgeAssessmentHistory(ctx context.Context, ps ...predicate.Assessment) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(assessment.FieldID).From(sql.Table(assessment.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.AssessmentHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(assessmenthistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging assessment history")

		return err
	}

	return nil
}

// PurgeAssessmentResponseHistory removes the history rows belonging to every assessmentresponse matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the assessmentresponse records themselves are deleted, the rows are matched
// with a sub-select against the assessmentresponse table
func PurgeAssessmentResponseHistory(ctx context.Context, ps ...predicate.AssessmentResponse) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(assessmentresponse.FieldID).From(sql.Table(assessmentresponse.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.AssessmentResponseHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(assessmentresponsehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging assessmentresponse history")

		return err
	}

	return nil
}

// PurgeAssetHistory removes the history rows belonging to every asset matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the asset records themselves are deleted, the rows are matched
// with a sub-select against the asset table
func PurgeAssetHistory(ctx context.Context, ps ...predicate.Asset) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(asset.FieldID).From(sql.Table(asset.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.AssetHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(assethistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging asset history")

		return err
	}

	return nil
}

// PurgeAudienceHistory removes the history rows belonging to every audience matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the audience records themselves are deleted, the rows are matched
// with a sub-select against the audience table
func PurgeAudienceHistory(ctx context.Context, ps ...predicate.Audience) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(audience.FieldID).From(sql.Table(audience.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.AudienceHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(audiencehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging audience history")

		return err
	}

	return nil
}

// PurgeAudienceMemberHistory removes the history rows belonging to every audiencemember matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the audiencemember records themselves are deleted, the rows are matched
// with a sub-select against the audiencemember table
func PurgeAudienceMemberHistory(ctx context.Context, ps ...predicate.AudienceMember) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(audiencemember.FieldID).From(sql.Table(audiencemember.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.AudienceMemberHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(audiencememberhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging audiencemember history")

		return err
	}

	return nil
}

// PurgeCampaignHistory removes the history rows belonging to every campaign matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the campaign records themselves are deleted, the rows are matched
// with a sub-select against the campaign table
func PurgeCampaignHistory(ctx context.Context, ps ...predicate.Campaign) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(campaign.FieldID).From(sql.Table(campaign.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.CampaignHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(campaignhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging campaign history")

		return err
	}

	return nil
}

// PurgeCampaignTargetHistory removes the history rows belonging to every campaigntarget matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the campaigntarget records themselves are deleted, the rows are matched
// with a sub-select against the campaigntarget table
func PurgeCampaignTargetHistory(ctx context.Context, ps ...predicate.CampaignTarget) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(campaigntarget.FieldID).From(sql.Table(campaigntarget.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.CampaignTargetHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(campaigntargethistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging campaigntarget history")

		return err
	}

	return nil
}

// PurgeContactHistory removes the history rows belonging to every contact matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the contact records themselves are deleted, the rows are matched
// with a sub-select against the contact table
func PurgeContactHistory(ctx context.Context, ps ...predicate.Contact) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(contact.FieldID).From(sql.Table(contact.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ContactHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(contacthistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging contact history")

		return err
	}

	return nil
}

// PurgeControlHistory removes the history rows belonging to every control matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the control records themselves are deleted, the rows are matched
// with a sub-select against the control table
func PurgeControlHistory(ctx context.Context, ps ...predicate.Control) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(control.FieldID).From(sql.Table(control.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ControlHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(controlhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging control history")

		return err
	}

	return nil
}

// PurgeControlImplementationHistory removes the history rows belonging to every controlimplementation matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the controlimplementation records themselves are deleted, the rows are matched
// with a sub-select against the controlimplementation table
func PurgeControlImplementationHistory(ctx context.Context, ps ...predicate.ControlImplementation) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(controlimplementation.FieldID).From(sql.Table(controlimplementation.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ControlImplementationHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(controlimplementationhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging controlimplementation history")

		return err
	}

	return nil
}

// PurgeControlObjectiveHistory removes the history rows belonging to every controlobjective matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the controlobjective records themselves are deleted, the rows are matched
// with a sub-select against the controlobjective table
func PurgeControlObjectiveHistory(ctx context.Context, ps ...predicate.ControlObjective) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(controlobjective.FieldID).From(sql.Table(controlobjective.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ControlObjectiveHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(controlobjectivehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging controlobjective history")

		return err
	}

	return nil
}

// PurgeCustomDomainHistory removes the history rows belonging to every customdomain matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the customdomain records themselves are deleted, the rows are matched
// with a sub-select against the customdomain table
func PurgeCustomDomainHistory(ctx context.Context, ps ...predicate.CustomDomain) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(customdomain.FieldID).From(sql.Table(customdomain.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.CustomDomainHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(customdomainhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging customdomain history")

		return err
	}

	return nil
}

// PurgeDiscussionHistory removes the history rows belonging to every discussion matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the discussion records themselves are deleted, the rows are matched
// with a sub-select against the discussion table
func PurgeDiscussionHistory(ctx context.Context, ps ...predicate.Discussion) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(discussion.FieldID).From(sql.Table(discussion.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.DiscussionHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(discussionhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging discussion history")

		return err
	}

	return nil
}

// PurgeDocumentDataHistory removes the history rows belonging to every documentdata matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the documentdata records themselves are deleted, the rows are matched
// with a sub-select against the documentdata table
func PurgeDocumentDataHistory(ctx context.Context, ps ...predicate.DocumentData) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(documentdata.FieldID).From(sql.Table(documentdata.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.DocumentDataHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(documentdatahistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging documentdata history")

		return err
	}

	return nil
}

// PurgeEmailTemplateHistory removes the history rows belonging to every emailtemplate matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the emailtemplate records themselves are deleted, the rows are matched
// with a sub-select against the emailtemplate table
func PurgeEmailTemplateHistory(ctx context.Context, ps ...predicate.EmailTemplate) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(emailtemplate.FieldID).From(sql.Table(emailtemplate.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.EmailTemplateHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(emailtemplatehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging emailtemplate history")

		return err
	}

	return nil
}

// PurgeEntityHistory removes the history rows belonging to every entity matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the entity records themselves are deleted, the rows are matched
// with a sub-select against the entity table
func PurgeEntityHistory(ctx context.Context, ps ...predicate.Entity) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(entity.FieldID).From(sql.Table(entity.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.EntityHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(entityhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging entity history")

		return err
	}

	return nil
}

// PurgeEntityTypeHistory removes the history rows belonging to every entitytype matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the entitytype records themselves are deleted, the rows are matched
// with a sub-select against the entitytype table
func PurgeEntityTypeHistory(ctx context.Context, ps ...predicate.EntityType) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(entitytype.FieldID).From(sql.Table(entitytype.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.EntityTypeHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(entitytypehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging entitytype history")

		return err
	}

	return nil
}

// PurgeEvidenceHistory removes the history rows belonging to every evidence matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the evidence records themselves are deleted, the rows are matched
// with a sub-select against the evidence table
func PurgeEvidenceHistory(ctx context.Context, ps ...predicate.Evidence) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(evidence.FieldID).From(sql.Table(evidence.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.EvidenceHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(evidencehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging evidence history")

		return err
	}

	return nil
}

// PurgeFileHistory removes the history rows belonging to every file matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the file records themselves are deleted, the rows are matched
// with a sub-select against the file table
func PurgeFileHistory(ctx context.Context, ps ...predicate.File) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(file.FieldID).From(sql.Table(file.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.FileHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(filehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging file history")

		return err
	}

	return nil
}

// PurgeFindingHistory removes the history rows belonging to every finding matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the finding records themselves are deleted, the rows are matched
// with a sub-select against the finding table
func PurgeFindingHistory(ctx context.Context, ps ...predicate.Finding) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(finding.FieldID).From(sql.Table(finding.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.FindingHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(findinghistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging finding history")

		return err
	}

	return nil
}

// PurgeFindingControlHistory removes the history rows belonging to every findingcontrol matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the findingcontrol records themselves are deleted, the rows are matched
// with a sub-select against the findingcontrol table
func PurgeFindingControlHistory(ctx context.Context, ps ...predicate.FindingControl) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(findingcontrol.FieldID).From(sql.Table(findingcontrol.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.FindingControlHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(findingcontrolhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging findingcontrol history")

		return err
	}

	return nil
}

// PurgeGroupHistory removes the history rows belonging to every group matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the group records themselves are deleted, the rows are matched
// with a sub-select against the group table
func PurgeGroupHistory(ctx context.Context, ps ...predicate.Group) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(group.FieldID).From(sql.Table(group.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.GroupHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(grouphistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging group history")

		return err
	}

	return nil
}

// PurgeGroupMembershipHistory removes the history rows belonging to every groupmembership matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the groupmembership records themselves are deleted, the rows are matched
// with a sub-select against the groupmembership table
func PurgeGroupMembershipHistory(ctx context.Context, ps ...predicate.GroupMembership) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(groupmembership.FieldID).From(sql.Table(groupmembership.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.GroupMembershipHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(groupmembershiphistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging groupmembership history")

		return err
	}

	return nil
}

// PurgeGroupSettingHistory removes the history rows belonging to every groupsetting matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the groupsetting records themselves are deleted, the rows are matched
// with a sub-select against the groupsetting table
func PurgeGroupSettingHistory(ctx context.Context, ps ...predicate.GroupSetting) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(groupsetting.FieldID).From(sql.Table(groupsetting.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.GroupSettingHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(groupsettinghistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging groupsetting history")

		return err
	}

	return nil
}

// PurgeHushHistory removes the history rows belonging to every hush matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the hush records themselves are deleted, the rows are matched
// with a sub-select against the hush table
func PurgeHushHistory(ctx context.Context, ps ...predicate.Hush) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(hush.FieldID).From(sql.Table(hush.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.HushHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(hushhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging hush history")

		return err
	}

	return nil
}

// PurgeIdentityHolderHistory removes the history rows belonging to every identityholder matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the identityholder records themselves are deleted, the rows are matched
// with a sub-select against the identityholder table
func PurgeIdentityHolderHistory(ctx context.Context, ps ...predicate.IdentityHolder) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(identityholder.FieldID).From(sql.Table(identityholder.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.IdentityHolderHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(identityholderhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging identityholder history")

		return err
	}

	return nil
}

// PurgeInternalPolicyHistory removes the history rows belonging to every internalpolicy matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the internalpolicy records themselves are deleted, the rows are matched
// with a sub-select against the internalpolicy table
func PurgeInternalPolicyHistory(ctx context.Context, ps ...predicate.InternalPolicy) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(internalpolicy.FieldID).From(sql.Table(internalpolicy.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.InternalPolicyHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(internalpolicyhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging internalpolicy history")

		return err
	}

	return nil
}

// PurgeMappableDomainHistory removes the history rows belonging to every mappabledomain matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the mappabledomain records themselves are deleted, the rows are matched
// with a sub-select against the mappabledomain table
func PurgeMappableDomainHistory(ctx context.Context, ps ...predicate.MappableDomain) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(mappabledomain.FieldID).From(sql.Table(mappabledomain.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.MappableDomainHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(mappabledomainhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging mappabledomain history")

		return err
	}

	return nil
}

// PurgeMappedControlHistory removes the history rows belonging to every mappedcontrol matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the mappedcontrol records themselves are deleted, the rows are matched
// with a sub-select against the mappedcontrol table
func PurgeMappedControlHistory(ctx context.Context, ps ...predicate.MappedControl) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(mappedcontrol.FieldID).From(sql.Table(mappedcontrol.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.MappedControlHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(mappedcontrolhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging mappedcontrol history")

		return err
	}

	return nil
}

// PurgeNarrativeHistory removes the history rows belonging to every narrative matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the narrative records themselves are deleted, the rows are matched
// with a sub-select against the narrative table
func PurgeNarrativeHistory(ctx context.Context, ps ...predicate.Narrative) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(narrative.FieldID).From(sql.Table(narrative.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.NarrativeHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(narrativehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging narrative history")

		return err
	}

	return nil
}

// PurgeNoteHistory removes the history rows belonging to every note matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the note records themselves are deleted, the rows are matched
// with a sub-select against the note table
func PurgeNoteHistory(ctx context.Context, ps ...predicate.Note) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(note.FieldID).From(sql.Table(note.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.NoteHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(notehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging note history")

		return err
	}

	return nil
}

// PurgeNotificationPreferenceHistory removes the history rows belonging to every notificationpreference matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the notificationpreference records themselves are deleted, the rows are matched
// with a sub-select against the notificationpreference table
func PurgeNotificationPreferenceHistory(ctx context.Context, ps ...predicate.NotificationPreference) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(notificationpreference.FieldID).From(sql.Table(notificationpreference.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.NotificationPreferenceHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(notificationpreferencehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging notificationpreference history")

		return err
	}

	return nil
}

// PurgeNotificationTemplateHistory removes the history rows belonging to every notificationtemplate matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the notificationtemplate records themselves are deleted, the rows are matched
// with a sub-select against the notificationtemplate table
func PurgeNotificationTemplateHistory(ctx context.Context, ps ...predicate.NotificationTemplate) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(notificationtemplate.FieldID).From(sql.Table(notificationtemplate.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.NotificationTemplateHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(notificationtemplatehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging notificationtemplate history")

		return err
	}

	return nil
}

// PurgeOrgMembershipHistory removes the history rows belonging to every orgmembership matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the orgmembership records themselves are deleted, the rows are matched
// with a sub-select against the orgmembership table
func PurgeOrgMembershipHistory(ctx context.Context, ps ...predicate.OrgMembership) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(orgmembership.FieldID).From(sql.Table(orgmembership.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.OrgMembershipHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(orgmembershiphistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging orgmembership history")

		return err
	}

	return nil
}

// PurgeOrganizationHistory removes the history rows belonging to every organization matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the organization records themselves are deleted, the rows are matched
// with a sub-select against the organization table
func PurgeOrganizationHistory(ctx context.Context, ps ...predicate.Organization) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(organization.FieldID).From(sql.Table(organization.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.OrganizationHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(organizationhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging organization history")

		return err
	}

	return nil
}

// PurgeOrganizationSettingHistory removes the history rows belonging to every organizationsetting matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the organizationsetting records themselves are deleted, the rows are matched
// with a sub-select against the organizationsetting table
func PurgeOrganizationSettingHistory(ctx context.Context, ps ...predicate.OrganizationSetting) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(organizationsetting.FieldID).From(sql.Table(organizationsetting.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.OrganizationSettingHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(organizationsettinghistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging organizationsetting history")

		return err
	}

	return nil
}

// PurgePlatformHistory removes the history rows belonging to every platform matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the platform records themselves are deleted, the rows are matched
// with a sub-select against the platform table
func PurgePlatformHistory(ctx context.Context, ps ...predicate.Platform) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(platform.FieldID).From(sql.Table(platform.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.PlatformHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(platformhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging platform history")

		return err
	}

	return nil
}

// PurgeProcedureHistory removes the history rows belonging to every procedure matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the procedure records themselves are deleted, the rows are matched
// with a sub-select against the procedure table
func PurgeProcedureHistory(ctx context.Context, ps ...predicate.Procedure) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(procedure.FieldID).From(sql.Table(procedure.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ProcedureHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(procedurehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging procedure history")

		return err
	}

	return nil
}

// PurgeProgramHistory removes the history rows belonging to every program matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the program records themselves are deleted, the rows are matched
// with a sub-select against the program table
func PurgeProgramHistory(ctx context.Context, ps ...predicate.Program) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(program.FieldID).From(sql.Table(program.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ProgramHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(programhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging program history")

		return err
	}

	return nil
}

// PurgeProgramMembershipHistory removes the history rows belonging to every programmembership matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the programmembership records themselves are deleted, the rows are matched
// with a sub-select against the programmembership table
func PurgeProgramMembershipHistory(ctx context.Context, ps ...predicate.ProgramMembership) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(programmembership.FieldID).From(sql.Table(programmembership.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ProgramMembershipHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(programmembershiphistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging programmembership history")

		return err
	}

	return nil
}

// PurgeRemediationHistory removes the history rows belonging to every remediation matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the remediation records themselves are deleted, the rows are matched
// with a sub-select against the remediation table
func PurgeRemediationHistory(ctx context.Context, ps ...predicate.Remediation) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(remediation.FieldID).From(sql.Table(remediation.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.RemediationHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(remediationhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging remediation history")

		return err
	}

	return nil
}

// PurgeReviewHistory removes the history rows belonging to every review matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the review records themselves are deleted, the rows are matched
// with a sub-select against the review table
func PurgeReviewHistory(ctx context.Context, ps ...predicate.Review) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(review.FieldID).From(sql.Table(review.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.ReviewHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(reviewhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging review history")

		return err
	}

	return nil
}

// PurgeRiskHistory removes the history rows belonging to every risk matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the risk records themselves are deleted, the rows are matched
// with a sub-select against the risk table
func PurgeRiskHistory(ctx context.Context, ps ...predicate.Risk) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(risk.FieldID).From(sql.Table(risk.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.RiskHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(riskhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging risk history")

		return err
	}

	return nil
}

// PurgeSLADefinitionHistory removes the history rows belonging to every sladefinition matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the sladefinition records themselves are deleted, the rows are matched
// with a sub-select against the sladefinition table
func PurgeSLADefinitionHistory(ctx context.Context, ps ...predicate.SLADefinition) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(sladefinition.FieldID).From(sql.Table(sladefinition.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.SLADefinitionHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(sladefinitionhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging sladefinition history")

		return err
	}

	return nil
}

// PurgeStandardHistory removes the history rows belonging to every standard matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the standard records themselves are deleted, the rows are matched
// with a sub-select against the standard table
func PurgeStandardHistory(ctx context.Context, ps ...predicate.Standard) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(standard.FieldID).From(sql.Table(standard.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.StandardHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(standardhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging standard history")

		return err
	}

	return nil
}

// PurgeSubcontrolHistory removes the history rows belonging to every subcontrol matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the subcontrol records themselves are deleted, the rows are matched
// with a sub-select against the subcontrol table
func PurgeSubcontrolHistory(ctx context.Context, ps ...predicate.Subcontrol) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(subcontrol.FieldID).From(sql.Table(subcontrol.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.SubcontrolHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(subcontrolhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging subcontrol history")

		return err
	}

	return nil
}

// PurgeSubprocessorHistory removes the history rows belonging to every subprocessor matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the subprocessor records themselves are deleted, the rows are matched
// with a sub-select against the subprocessor table
func PurgeSubprocessorHistory(ctx context.Context, ps ...predicate.Subprocessor) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(subprocessor.FieldID).From(sql.Table(subprocessor.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.SubprocessorHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(subprocessorhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging subprocessor history")

		return err
	}

	return nil
}

// PurgeSystemDetailHistory removes the history rows belonging to every systemdetail matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the systemdetail records themselves are deleted, the rows are matched
// with a sub-select against the systemdetail table
func PurgeSystemDetailHistory(ctx context.Context, ps ...predicate.SystemDetail) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(systemdetail.FieldID).From(sql.Table(systemdetail.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.SystemDetailHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(systemdetailhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging systemdetail history")

		return err
	}

	return nil
}

// PurgeTaskHistory removes the history rows belonging to every task matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the task records themselves are deleted, the rows are matched
// with a sub-select against the task table
func PurgeTaskHistory(ctx context.Context, ps ...predicate.Task) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(task.FieldID).From(sql.Table(task.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TaskHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(taskhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging task history")

		return err
	}

	return nil
}

// PurgeTemplateHistory removes the history rows belonging to every template matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the template records themselves are deleted, the rows are matched
// with a sub-select against the template table
func PurgeTemplateHistory(ctx context.Context, ps ...predicate.Template) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(template.FieldID).From(sql.Table(template.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TemplateHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(templatehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging template history")

		return err
	}

	return nil
}

// PurgeTrustCenterHistory removes the history rows belonging to every trustcenter matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcenter records themselves are deleted, the rows are matched
// with a sub-select against the trustcenter table
func PurgeTrustCenterHistory(ctx context.Context, ps ...predicate.TrustCenter) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcenter.FieldID).From(sql.Table(trustcenter.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcenterhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcenter history")

		return err
	}

	return nil
}

// PurgeTrustCenterComplianceHistory removes the history rows belonging to every trustcentercompliance matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcentercompliance records themselves are deleted, the rows are matched
// with a sub-select against the trustcentercompliance table
func PurgeTrustCenterComplianceHistory(ctx context.Context, ps ...predicate.TrustCenterCompliance) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcentercompliance.FieldID).From(sql.Table(trustcentercompliance.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterComplianceHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcentercompliancehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcentercompliance history")

		return err
	}

	return nil
}

// PurgeTrustCenterDocHistory removes the history rows belonging to every trustcenterdoc matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcenterdoc records themselves are deleted, the rows are matched
// with a sub-select against the trustcenterdoc table
func PurgeTrustCenterDocHistory(ctx context.Context, ps ...predicate.TrustCenterDoc) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcenterdoc.FieldID).From(sql.Table(trustcenterdoc.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterDocHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcenterdochistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcenterdoc history")

		return err
	}

	return nil
}

// PurgeTrustCenterEntityHistory removes the history rows belonging to every trustcenterentity matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcenterentity records themselves are deleted, the rows are matched
// with a sub-select against the trustcenterentity table
func PurgeTrustCenterEntityHistory(ctx context.Context, ps ...predicate.TrustCenterEntity) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcenterentity.FieldID).From(sql.Table(trustcenterentity.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterEntityHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcenterentityhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcenterentity history")

		return err
	}

	return nil
}

// PurgeTrustCenterFAQHistory removes the history rows belonging to every trustcenterfaq matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcenterfaq records themselves are deleted, the rows are matched
// with a sub-select against the trustcenterfaq table
func PurgeTrustCenterFAQHistory(ctx context.Context, ps ...predicate.TrustCenterFAQ) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcenterfaq.FieldID).From(sql.Table(trustcenterfaq.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterFAQHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcenterfaqhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcenterfaq history")

		return err
	}

	return nil
}

// PurgeTrustCenterNDARequestHistory removes the history rows belonging to every trustcenterndarequest matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcenterndarequest records themselves are deleted, the rows are matched
// with a sub-select against the trustcenterndarequest table
func PurgeTrustCenterNDARequestHistory(ctx context.Context, ps ...predicate.TrustCenterNDARequest) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcenterndarequest.FieldID).From(sql.Table(trustcenterndarequest.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterNDARequestHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcenterndarequesthistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcenterndarequest history")

		return err
	}

	return nil
}

// PurgeTrustCenterSettingHistory removes the history rows belonging to every trustcentersetting matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcentersetting records themselves are deleted, the rows are matched
// with a sub-select against the trustcentersetting table
func PurgeTrustCenterSettingHistory(ctx context.Context, ps ...predicate.TrustCenterSetting) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcentersetting.FieldID).From(sql.Table(trustcentersetting.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterSettingHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcentersettinghistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcentersetting history")

		return err
	}

	return nil
}

// PurgeTrustCenterSubprocessorHistory removes the history rows belonging to every trustcentersubprocessor matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcentersubprocessor records themselves are deleted, the rows are matched
// with a sub-select against the trustcentersubprocessor table
func PurgeTrustCenterSubprocessorHistory(ctx context.Context, ps ...predicate.TrustCenterSubprocessor) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcentersubprocessor.FieldID).From(sql.Table(trustcentersubprocessor.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterSubprocessorHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcentersubprocessorhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcentersubprocessor history")

		return err
	}

	return nil
}

// PurgeTrustCenterWatermarkConfigHistory removes the history rows belonging to every trustcenterwatermarkconfig matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the trustcenterwatermarkconfig records themselves are deleted, the rows are matched
// with a sub-select against the trustcenterwatermarkconfig table
func PurgeTrustCenterWatermarkConfigHistory(ctx context.Context, ps ...predicate.TrustCenterWatermarkConfig) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(trustcenterwatermarkconfig.FieldID).From(sql.Table(trustcenterwatermarkconfig.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.TrustCenterWatermarkConfigHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(trustcenterwatermarkconfighistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging trustcenterwatermarkconfig history")

		return err
	}

	return nil
}

// PurgeUserHistory removes the history rows belonging to every user matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the user records themselves are deleted, the rows are matched
// with a sub-select against the user table
func PurgeUserHistory(ctx context.Context, ps ...predicate.User) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(user.FieldID).From(sql.Table(user.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.UserHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(userhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging user history")

		return err
	}

	return nil
}

// PurgeUserSettingHistory removes the history rows belonging to every usersetting matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the usersetting records themselves are deleted, the rows are matched
// with a sub-select against the usersetting table
func PurgeUserSettingHistory(ctx context.Context, ps ...predicate.UserSetting) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(usersetting.FieldID).From(sql.Table(usersetting.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.UserSettingHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(usersettinghistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging usersetting history")

		return err
	}

	return nil
}

// PurgeVendorRiskScoreHistory removes the history rows belonging to every vendorriskscore matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the vendorriskscore records themselves are deleted, the rows are matched
// with a sub-select against the vendorriskscore table
func PurgeVendorRiskScoreHistory(ctx context.Context, ps ...predicate.VendorRiskScore) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(vendorriskscore.FieldID).From(sql.Table(vendorriskscore.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.VendorRiskScoreHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(vendorriskscorehistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging vendorriskscore history")

		return err
	}

	return nil
}

// PurgeVendorScoringConfigHistory removes the history rows belonging to every vendorscoringconfig matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the vendorscoringconfig records themselves are deleted, the rows are matched
// with a sub-select against the vendorscoringconfig table
func PurgeVendorScoringConfigHistory(ctx context.Context, ps ...predicate.VendorScoringConfig) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(vendorscoringconfig.FieldID).From(sql.Table(vendorscoringconfig.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.VendorScoringConfigHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(vendorscoringconfighistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging vendorscoringconfig history")

		return err
	}

	return nil
}

// PurgeVulnerabilityHistory removes the history rows belonging to every vulnerability matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the vulnerability records themselves are deleted, the rows are matched
// with a sub-select against the vulnerability table
func PurgeVulnerabilityHistory(ctx context.Context, ps ...predicate.Vulnerability) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(vulnerability.FieldID).From(sql.Table(vulnerability.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.VulnerabilityHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(vulnerabilityhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging vulnerability history")

		return err
	}

	return nil
}

// PurgeWorkflowAssignmentHistory removes the history rows belonging to every workflowassignment matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the workflowassignment records themselves are deleted, the rows are matched
// with a sub-select against the workflowassignment table
func PurgeWorkflowAssignmentHistory(ctx context.Context, ps ...predicate.WorkflowAssignment) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(workflowassignment.FieldID).From(sql.Table(workflowassignment.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.WorkflowAssignmentHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(workflowassignmenthistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging workflowassignment history")

		return err
	}

	return nil
}

// PurgeWorkflowAssignmentTargetHistory removes the history rows belonging to every workflowassignmenttarget matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the workflowassignmenttarget records themselves are deleted, the rows are matched
// with a sub-select against the workflowassignmenttarget table
func PurgeWorkflowAssignmentTargetHistory(ctx context.Context, ps ...predicate.WorkflowAssignmentTarget) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(workflowassignmenttarget.FieldID).From(sql.Table(workflowassignmenttarget.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.WorkflowAssignmentTargetHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(workflowassignmenttargethistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging workflowassignmenttarget history")

		return err
	}

	return nil
}

// PurgeWorkflowDefinitionHistory removes the history rows belonging to every workflowdefinition matching
// the given predicates. It is a no-op unless the context opts in via contextx.WithPurgeHistory, so
// deletes that should keep their audit trail are unaffected.
// This has to run before the workflowdefinition records themselves are deleted, the rows are matched
// with a sub-select against the workflowdefinition table
func PurgeWorkflowDefinitionHistory(ctx context.Context, ps ...predicate.WorkflowDefinition) error {
	if !contextx.PurgeHistoryEnabled(ctx) {
		return nil
	}

	client := FromContext(ctx)
	if client == nil || client.HistoryClient == nil {
		return nil
	}

	refs := sql.Select(workflowdefinition.FieldID).From(sql.Table(workflowdefinition.Table))
	for _, p := range ps {
		p(refs)
	}

	if _, err := client.HistoryClient.WorkflowDefinitionHistory.Delete().Where(func(s *sql.Selector) {
		s.Where(sql.In(workflowdefinitionhistory.FieldRef, refs))
	}).Exec(history.WithContext(ctx)); err != nil {
		logx.FromContext(ctx).Error().Err(err).Msg("error purging workflowdefinition history")

		return err
	}

	return nil
}
