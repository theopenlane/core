//go:build examples

// Package campaign exposes subcommands for creating campaigns (with targets)
// and launching them — the end-to-end path that dispatches branded emails
// to recipients via the configured email integration.
package campaign
