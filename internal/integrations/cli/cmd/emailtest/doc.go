// Package emailtest provides CLI commands for sending test emails through
// registered email dispatchers, bypassing the full integration runtime to
// enable template and rendering verification without database dependencies.
package emailtest
