# TODO

- Add CC / BCC configuration and thread through send ability
- Add logo upload / hosting (and re-sizing)
- Add file upload + attachment + presigned URL generation
- per-recipient or per-campaign interpolation in the subject (the plumbing for it (ExecuteTextTemplate + a defined variable contract) would need to be added to the EmailOperation.dispatch path)
