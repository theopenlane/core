// Package schema defines data model types for integration installation records.
// These types are stored as typed JSON fields in the ent schema and must not
// import ent/generated to avoid import cycles.
package schema
