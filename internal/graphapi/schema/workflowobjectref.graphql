extend type Query {
    """
    Look up workflowObjectRef by ID
    """
     workflowObjectRef(
        """
        ID of the workflowObjectRef
        """
        id: ID!
    ):  WorkflowObjectRef!
}
